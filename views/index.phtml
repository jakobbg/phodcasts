<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="color-scheme" content="light dark">
  <title><?= h(APP_NAME) ?></title>

  <!-- Favicon & home-screen icon -->
  <link rel="icon" type="image/png" href="<?= h($faviconUrl) ?>">
  <link rel="apple-touch-icon" href="<?= h($iconUrl) ?>">
  <link rel="apple-touch-icon-precomposed" href="<?= h($assetBase . 'apple-touch-icon-precomposed.png') ?>">
  <meta name="apple-mobile-web-app-title" content="<?= h(APP_NAME) ?>">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">

  <!-- Open Graph - iMessage / Slack / Discord link previews -->
  <meta property="og:type"        content="website">
  <meta property="og:site_name"   content="<?= h(APP_NAME) ?>">
  <meta property="og:title"       content="<?= h(APP_NAME . ': ' . APP_QUIP) ?>">
  <meta property="og:description" content="Your podcasts &amp; audiobooks, streamed from your own server. <?= h(APP_QUIP) ?>">
  <meta property="og:url"         content="<?= h($base) ?>">
  <meta property="og:image"       content="<?= h($ogImageUrl) ?>">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">
  <meta property="og:image:type"  content="image/png">

  <!-- Twitter / X card (also used by some other clients) -->
  <meta name="twitter:card"        content="summary_large_image">
  <meta name="twitter:title"       content="<?= h(APP_NAME . ': ' . APP_QUIP) ?>">
  <meta name="twitter:description" content="Your podcasts &amp; audiobooks, streamed from your own server. <?= h(APP_QUIP) ?>">
  <meta name="twitter:image"       content="<?= h($ogImageUrl) ?>">
  <script>(function(){var t=localStorage.getItem('theme');if(t)document.documentElement.setAttribute('data-theme',t);}());</script>
  <style>
  :root {
    --bg: #0b1220;
    --bg2: #0d1b2e;
    --card: rgba(255,255,255,.08);
    --cardBorder: rgba(255,255,255,.14);
    --text: rgba(255,255,255,.92);
    --muted: rgba(255,255,255,.70);
    --shadow: rgba(0,0,0,.28);
    --accent: #7c3aed;
    --accent2: #06b6d4;
    --radius: 18px;

    --font-sans: ui-sans-serif, system-ui, -apple-system, "SF Pro Text", "Segoe UI Variable", "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
    --font-serif: ui-serif, "New York", "Iowan Old Style", "Palatino Linotype", Palatino, Georgia, serif;
  }

  @media (prefers-color-scheme: light) {
    html:not([data-theme="dark"]) {
      --bg: #f6f7fb;
      --bg2: #eef2ff;
      --card: rgba(255,255,255,.75);
      --cardBorder: rgba(15,23,42,.12);
      --text: rgba(15,23,42,.92);
      --muted: rgba(15,23,42,.66);
      --shadow: rgba(15,23,42,.14);
    }
  }
  html[data-theme="light"] {
    --bg: #f6f7fb;
    --bg2: #eef2ff;
    --card: rgba(255,255,255,.75);
    --cardBorder: rgba(15,23,42,.12);
    --text: rgba(15,23,42,.92);
    --muted: rgba(15,23,42,.66);
    --shadow: rgba(15,23,42,.14);
  }

  * { box-sizing: border-box; }
  html, body { height: 100%; }
  body {
    margin: 0;
    font-family: var(--font-sans);
    line-height: 1.45;
    color: var(--text);
    background:
      radial-gradient(1200px 500px at 10% 0%, rgba(124,58,237,.35), transparent 60%),
      radial-gradient(900px 500px at 90% 10%, rgba(6,182,212,.25), transparent 55%),
      linear-gradient(180deg, var(--bg), var(--bg2));
    background-attachment: fixed;
  }

  a { color: inherit; text-decoration: none; }
  a:focus-visible { outline: 2px solid rgba(124,58,237,.55); outline-offset: 3px; border-radius: 10px; }

  /* Skip-to-content link: visually hidden until focused */
  .skip-link {
    position: absolute;
    width: 1px; height: 1px;
    padding: 0; margin: -1px;
    overflow: hidden; clip: rect(0,0,0,0);
    white-space: nowrap; border: 0;
  }
  .skip-link:focus-visible {
    position: fixed;
    top: 10px; left: 10px;
    width: auto; height: auto;
    padding: 10px 18px;
    clip: auto; overflow: visible;
    background: var(--accent); color: #fff;
    border-radius: 10px; font-weight: 700; font-size: 14px;
    z-index: 9999;
    outline: 2px solid #fff; outline-offset: 2px;
  }

  /* Visible focus rings for interactive elements */
  .btn:focus-visible, .filter-btn:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 3px;
  }
  .btn.primary:focus-visible { outline-color: #fff; }

  /* Respect reduced-motion preference */
  @media (prefers-reduced-motion: reduce) {
    .btn, .filter-btn { transition: none; }
  }

  .wrap {
    max-width: 980px;
    margin: 0 auto;
    padding: 40px 20px 56px;
  }

  .hero {
    margin-bottom: 22px;
    padding: 22px 22px 18px;
    border: 1px solid var(--cardBorder);
    background: linear-gradient(180deg, rgba(255,255,255,.10), rgba(255,255,255,.06));
    box-shadow: 0 18px 60px var(--shadow);
    border-radius: var(--radius);
    backdrop-filter: blur(10px);
  }

  h1 {
    margin: 0 0 8px;
    font-family: var(--font-serif);
    font-weight: 700;
    letter-spacing: .2px;
    font-size: clamp(28px, 4vw, 40px);
  }

  .sub {
    margin: 0;
    color: var(--muted);
    font-size: 15px;
  }

  code {
    font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
    font-size: 0.95em;
    padding: 0.18rem 0.42rem;
    border-radius: 10px;
    border: 1px solid var(--cardBorder);
    background: rgba(255,255,255,.08);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 14px;
    margin-top: 14px;
  }
  .grid.single-visible {
    grid-template-columns: repeat(2, minmax(260px, 1fr));
  }

  .card {
    border: 1px solid var(--cardBorder);
    background: var(--card);
    border-radius: var(--radius);
    padding: 16px 16px 14px;
    box-shadow: 0 16px 44px var(--shadow);
    backdrop-filter: blur(10px);
    display: flex;
    flex-direction: column;
  }

  .cover {
    width: 100%;
    aspect-ratio: 1 / 1;
    object-fit: cover;
    border-radius: 14px;
    border: 1px solid var(--cardBorder);
    display: block;
    background: rgba(255,255,255,.06);
  }

  /* Cover wrapper — positions the RSS copy icon over the artwork */
  .cover-wrap { position: relative; margin-bottom: 12px; }
  .cover-copy-btn {
    position: absolute; bottom: 8px; right: 8px;
    width: 30px; height: 30px; border-radius: 50%;
    border: 1px solid rgba(255,255,255,.25);
    background: rgba(0,0,0,.45); backdrop-filter: blur(6px);
    color: rgba(255,255,255,.85); cursor: pointer;
    display: inline-flex; align-items: center; justify-content: center;
    transition: background .12s, color .12s, border-color .12s;
  }
  .cover-copy-btn:hover { background: rgba(0,0,0,.65); color: #fff; }
  .cover-copy-btn.copied { color: #4ade80; border-color: #4ade80; }
  .cover-copy-btn:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }

  .pod-title {
    font-family: var(--font-sans);
    font-weight: 700;
    font-size: 18px;
    margin: 0 0 10px;
    letter-spacing: .2px;
  }

  .meta {
    margin: -4px 0 12px;
    color: var(--muted);
    font-size: 13px;
  }

  .actions {
    display: flex;
    gap: 8px;
    flex-wrap: nowrap;
    margin-top: auto;
    overflow: hidden;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 12px;
    border-radius: 12px;
    border: 1px solid var(--cardBorder);
    background: rgba(255,255,255,.06);
    color: var(--text);
    font-weight: 650;
    font-size: 14px;
    transition: transform .06s ease, background .15s ease, border-color .15s ease;
    user-select: none;
  }

  .btn:hover { background: rgba(255,255,255,.10); }

  .btn.primary {
    border-color: rgba(124,58,237,.55);
    background: linear-gradient(135deg, rgba(124,58,237,.92), rgba(6,182,212,.72));
    color: white;
  }

  .btn.primary:hover { background: linear-gradient(135deg, rgba(124,58,237,1), rgba(6,182,212,.85)); }

  .site-footer {
    margin-top: 18px;
    color: var(--muted);
    font-size: 14px;
  }
  .site-footer p { margin: 0; }

  .divider {
    height: 1px;
    margin: 18px 0 0;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,.22), transparent);
  }

  .filter-bar {
    display: flex;
    gap: 6px;
    margin-bottom: 16px;
  }

  .filter-btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 16px;
    border-radius: 999px;
    border: 1px solid var(--cardBorder);
    background: rgba(255,255,255,.06);
    color: var(--muted);
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    text-decoration: none;
    transition: background .15s ease, color .15s ease, border-color .15s ease;
  }

  .filter-btn:hover {
    background: rgba(255,255,255,.12);
    color: var(--text);
  }

  .filter-btn.active {
    background: linear-gradient(135deg, rgba(124,58,237,.85), rgba(6,182,212,.65));
    border-color: transparent;
    color: #fff;
  }

  .type-badge {
    display: inline-block;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: .4px;
    text-transform: uppercase;
    padding: 2px 8px;
    border-radius: 999px;
    margin-bottom: 8px;
  }

  .type-badge.podcast {
    background: rgba(124,58,237,.22);
    color: #a78bfa;
    border: 1px solid rgba(124,58,237,.35);
  }

  .type-badge.book {
    background: rgba(6,182,212,.18);
    color: #67e8f9;
    border: 1px solid rgba(6,182,212,.30);
  }

  @media (prefers-color-scheme: light) {
    html:not([data-theme="dark"]) .divider { background: linear-gradient(90deg, transparent, rgba(15,23,42,.18), transparent); }
    html:not([data-theme="dark"]) .hero { background: linear-gradient(180deg, rgba(255,255,255,.92), rgba(255,255,255,.75)); }
    html:not([data-theme="dark"]) code { background: rgba(15,23,42,.04); }
    html:not([data-theme="dark"]) .btn { background: rgba(255,255,255,.75); }
    html:not([data-theme="dark"]) .type-badge.podcast { background: rgba(124,58,237,.10); color: #6d28d9; border-color: rgba(124,58,237,.25); }
    html:not([data-theme="dark"]) .type-badge.book    { background: rgba(6,182,212,.10);  color: #0e7490; border-color: rgba(6,182,212,.25); }
    html:not([data-theme="dark"]) .filter-btn         { background: rgba(255,255,255,.75); }
  }
  html[data-theme="light"] .divider { background: linear-gradient(90deg, transparent, rgba(15,23,42,.18), transparent); }
  html[data-theme="light"] .hero { background: linear-gradient(180deg, rgba(255,255,255,.92), rgba(255,255,255,.75)); }
  html[data-theme="light"] code { background: rgba(15,23,42,.04); }
  html[data-theme="light"] .btn { background: rgba(255,255,255,.75); }
  html[data-theme="light"] .type-badge.podcast { background: rgba(124,58,237,.10); color: #6d28d9; border-color: rgba(124,58,237,.25); }
  html[data-theme="light"] .type-badge.book    { background: rgba(6,182,212,.10);  color: #0e7490; border-color: rgba(6,182,212,.25); }
  html[data-theme="light"] .filter-btn         { background: rgba(255,255,255,.75); }

  /* Theme toggle button */
  .theme-toggle {
    position: fixed; top: 14px; right: 16px; z-index: 50;
    display: inline-flex; align-items: center; justify-content: center;
    width: 34px; height: 34px; border-radius: 50%;
    border: 1px solid var(--cardBorder); background: var(--card);
    backdrop-filter: blur(10px); color: var(--muted);
    font-size: 16px; cursor: pointer;
    transition: background .12s, color .12s;
  }
  .theme-toggle:hover { background: rgba(255,255,255,.15); color: var(--text); }
  .theme-toggle:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }

  /* Search */
  .search-wrap { display: flex; align-items: center; margin-left: auto; position: relative; }
  .search-input {
    -webkit-appearance: none; appearance: none;
    padding: 7px 30px 7px 12px; border-radius: 999px;
    border: 1px solid var(--cardBorder); background: rgba(255,255,255,.06);
    color: var(--text); font-size: 14px; font-family: var(--font-sans);
    width: 160px; outline: none;
    transition: width .2s ease, border-color .15s, background .15s;
  }
  .search-input:focus { width: 220px; border-color: rgba(124,58,237,.55); background: rgba(255,255,255,.09); }
  .search-input::placeholder { color: var(--muted); }
  .search-action {
    position: absolute; right: 10px; background: none; border: none; padding: 0;
    color: var(--muted); cursor: pointer;
    display: inline-flex; align-items: center;
    font-size: 13px; line-height: 1; text-decoration: none;
  }
  .search-action:hover { color: var(--text); }
  @media (prefers-color-scheme: light) {
    html:not([data-theme="dark"]) .search-input { background: rgba(255,255,255,.75); }
  }
  html[data-theme="light"] .search-input { background: rgba(255,255,255,.75); }
  @media (max-width: 500px) {
    .search-wrap { margin-left: 0; }
    .search-input, .search-input:focus { width: 100%; }
  }
  </style>
  <script defer src="<?= h($assetBase . 'js/theme.js') ?>"></script>
</head>
<body>
  <button id="theme-toggle" class="theme-toggle" aria-label="Switch to light theme">&#x2299;</button>
  <a href="#main-content" class="skip-link">Skip to main content</a>
  <div class="wrap">
    <header class="hero">
      <?php
        $commitHash = app_commit_hash();
        $shortHash  = $commitHash !== '' ? substr($commitHash, 0, 7) : '';
        $versionTip = APP_NAME . ' ' . APP_VERSION . ($shortHash !== '' ? ' (' . $shortHash . ')' : '');
        $commitUrl  = REPO_URL . ($commitHash !== '' ? '/commit/' . $commitHash : '');
      ?>
      <h1 id="app-title"
          title="<?= h($versionTip) ?>"
          style="cursor:pointer"
          aria-label="<?= h($versionTip) ?>, click to copy commit link"
          ><span id="app-brand"><?= h(APP_NAME) ?></span> <span style="font-weight:400;opacity:.6">&mdash; Podcast &amp; Audiobook Feeds</span></h1>
      <p class="sub"><?= h(APP_QUIP) ?></p>
    </header>

    <?php
      $filterLinks = [
        'all'      => ['icon' => '🎙+📚', 'text' => 'All',         'title' => 'Show everything'],
        'podcasts' => ['icon' => '🎙',     'text' => 'Podcasts',    'title' => 'Show podcasts only'],
        'books'    => ['icon' => '📚',     'text' => 'Audio Books', 'title' => 'Show audio books only'],
      ];
    ?>
    <nav class="filter-bar" aria-label="Content type filter">
      <?php foreach ($filterLinks as $val => $info):
        $href = $base . '?' . http_build_query(['filter' => $val]);
        $active = ($filter === $val) ? ' active' : '';
        $aria = $filter === $val ? ' aria-current="page"' : '';
      ?>
        <a class="filter-btn<?php echo $active; ?>" href="<?php echo h($href); ?>" title="<?php echo h($info['title']); ?>"<?php echo $aria; ?>><span aria-hidden="true"><?php echo $info['icon']; ?></span> <?php echo h($info['text']); ?></a>
      <?php endforeach; ?>
      <div class="search-wrap" role="search">
        <input type="search" id="feed-search" class="search-input"
               placeholder="Search…" value="<?php echo h($query); ?>"
               autocomplete="off" spellcheck="false" aria-label="Search feeds">
        <button type="button" id="search-clear" class="search-action search-clear" aria-label="Clear search"<?php echo $query === '' ? ' hidden' : ''; ?>>&#x2715;</button>
      </div>
    </nav>

    <main id="main-content">

    <?php if (empty($feeds)): ?>
      <div class="card">No subfolders found (or not readable) for the selected filter.</div>
    <?php else: ?>
      <div class="grid">
        <?php foreach ($feeds as $f):
          $rss = $base . '?' . http_build_query(['feed' => $f['id']]);
          // Build a clean path URL (no query string) for the podcast:// scheme.
          // Browsers pass custom URL schemes to the OS far more reliably when
          // the URL is a simple path - query strings are routinely dropped by
          // Chrome and others before handing off to the native app handler.
          $encodedId    = implode('/', array_map('rawurlencode', explode('/', $f['id'])));
          $cleanFeedUrl = $assetBase . 'feed/' . $encodedId;
          $podcastUrl   = preg_replace('#^https?://#i', 'podcast://', $cleanFeedUrl);
          // Encode the current page state so the show page can return here.
          $backParams = [];
          if ($filter !== 'all') $backParams['filter'] = $filter;
          if ($query !== '')     $backParams['q']      = $query;
          $returnTo = $assetBase . ($backParams ? '?' . http_build_query($backParams) : '');
          $showUrl  = $assetBase . 'show/' . $encodedId . '?return_to=' . rawurlencode($returnTo);

          $stats = podcast_stats($f['dir']);
          $episodeCount = (int)$stats['count'];

          $newestTs = !empty($stats['newest_ts']) ? (int)$stats['newest_ts'] : null;
          $newestIso = $newestTs !== null ? gmdate('Y-m-d', $newestTs) : null;
          $newestHuman = $newestTs !== null ? human_age($newestTs) : null;

          $coverImgPath = discover_image($f['dir']);
          $coverUrl = $coverImgPath !== null
              ? media_url($f['id'], basename($coverImgPath))
              : null;
          $badgeLabel = $f['type'] === 'book' ? 'Audio Book' : 'Podcast';
          $badgeEmoji = $f['type'] === 'book' ? '📚' : '🎙';
          $badgeClass = $f['type'] === 'book' ? 'book' : 'podcast';
        ?>
          <section class="card" aria-label="<?php echo h($f['name']); ?>" data-feed-id="<?php echo h($f['id']); ?>">
            <div class="cover-wrap">
              <a href="<?php echo h($showUrl); ?>" aria-label="Open details for <?php echo h($f['name']); ?>">
                <?php if ($coverUrl !== null): ?>
                  <img class="cover" src="<?php echo h($coverUrl); ?>" alt="Cover art for <?php echo h($f['name']); ?>" loading="lazy" />
                <?php else: ?>
                  <?php echo cover_placeholder_svg($f['name'], $f['name']); ?>
                <?php endif; ?>
              </a>
              <button class="cover-copy-btn" onclick="copyRssFeed(this,'<?php echo h(addslashes($rss)); ?>')" title="Copy RSS feed URL" aria-label="Copy RSS feed URL for <?php echo h($f['name']); ?>"><svg viewBox="0 0 24 24" width="14" height="14" fill="currentColor"><path d="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z"/></svg></button>
            </div>
            <span class="type-badge <?php echo $badgeClass; ?>"><span aria-hidden="true"><?php echo $badgeEmoji; ?></span> <?php echo h($badgeLabel); ?></span>
            <h2 class="pod-title"><?php echo h($f['name']); ?></h2>
            <div class="meta">
              <?php echo (int)$episodeCount; ?> <?php echo $episodeCount === 1 ? 'episode' : 'episodes'; ?><?php if ($newestHuman !== null): ?> &middot; Newest: <time datetime="<?php echo h((string)$newestIso); ?>"><?php echo h($newestHuman); ?></time><?php endif; ?>
            </div>
            <div class="actions">
              <a href="<?php echo h($showUrl); ?>" class="btn" aria-label="View episodes for <?php echo h($f['name']); ?>">Details</a>
              <a href="<?php echo h($podcastUrl); ?>" class="btn primary" aria-label="Subscribe to <?php echo h($f['name']); ?>">Subscribe</a>
            </div>
          </section>
        <?php endforeach; ?>
      </div>
      <div id="filter-empty" class="card" style="display:none; margin-top: 14px;">No visible feeds match this search.</div>
    <?php endif; ?>

    </main>

    <div class="divider"></div>
    <footer class="site-footer">
      <p>Tap <strong>Apple Podcasts</strong> to subscribe directly. Use the <strong>clipboard icon</strong> on each cover to copy the RSS URL into any other podcast app.</p>
    </footer>
  </div>

<script>
function copyRssFeed(btn, url) {
  var SVG_COPY  = btn.innerHTML;
  var SVG_CHECK = '<svg viewBox="0 0 24 24" width="14" height="14" fill="currentColor"><path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41z"/></svg>';
  if (!navigator.clipboard) { prompt('Copy this feed URL:', url); return; }
  navigator.clipboard.writeText(url).then(function () {
    btn.innerHTML = SVG_CHECK;
    btn.classList.add('copied');
    setTimeout(function () { btn.innerHTML = SVG_COPY; btn.classList.remove('copied'); }, 2000);
  });
}
function copyFeed(btn, url) {
  if (!navigator.clipboard) { prompt('Copy this feed URL:', url); return; }
  navigator.clipboard.writeText(url).then(function () {
    var orig = btn.textContent, origLabel = btn.getAttribute('aria-label');
    btn.style.width = btn.offsetWidth + 'px';
    btn.textContent = 'Copied!';
    btn.setAttribute('aria-label', 'Copied to clipboard');
    setTimeout(function () { btn.style.width = ''; btn.textContent = orig; btn.setAttribute('aria-label', origLabel); }, 2000);
  });
}
(function () {
  // Copy GitHub commit URL when the app title is clicked.
  var titleEl = document.getElementById('app-title');
  if (titleEl) {
    titleEl.addEventListener('click', function () {
      var url = '<?= h($commitUrl) ?>';
      if (!url) return;
      if (!navigator.clipboard) { prompt('Commit URL:', url); return; }
      navigator.clipboard.writeText(url).then(function () {
        var brand = document.getElementById('app-brand');
        var origText  = brand ? brand.textContent : '';
        var origTitle = titleEl.getAttribute('title');
        if (brand) brand.textContent = 'Copied!';
        setTimeout(function () {
          if (brand) brand.textContent = origText;
          titleEl.setAttribute('title', origTitle);
        }, 2000);
      });
    });
  }
}());
(function () {
  var inp = document.getElementById('feed-search');
  var clearBtn = document.getElementById('search-clear');
  var noMatch = document.getElementById('filter-empty');
  var grid = document.querySelector('.grid');
  if (!inp) return;
  function filterCards(q) {
    q = q.toLowerCase().trim();
    var visible = 0;
    document.querySelectorAll('.grid section.card').forEach(function (card) {
      var h2 = card.querySelector('h2');
      var title = h2 ? h2.textContent.toLowerCase() : '';
      var show = !q || title.indexOf(q) !== -1;
      card.style.display = show ? '' : 'none';
      if (show) visible += 1;
    });
    if (clearBtn) clearBtn.hidden = q === '';
    if (noMatch) noMatch.style.display = (q !== '' && visible === 0) ? '' : 'none';
    if (grid) grid.classList.toggle('single-visible', visible === 1);
  }
  inp.addEventListener('input', function () {
    filterCards(inp.value);
  });
  if (clearBtn) {
    clearBtn.addEventListener('click', function () {
      inp.value = '';
      filterCards('');
      inp.focus();
    });
  }
  if (inp.value) filterCards(inp.value);
}());
// Remember current page URL so the show detail page can navigate back here.
(function () {
  document.querySelectorAll('a.btn[href*="/show/"]').forEach(function (a) {
    a.addEventListener('click', function () {
      sessionStorage.setItem('ph-back', window.location.href);
    });
  });
}());
// Scroll to the previously viewed show card when returning from a show page.
(function () {
  var feed = sessionStorage.getItem('ph-scroll');
  if (!feed) return;
  sessionStorage.removeItem('ph-scroll');
  var card = document.querySelector('[data-feed-id=' + JSON.stringify(feed) + ']');
  if (!card) return;
  requestAnimationFrame(function () {
    card.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
    card.style.outline = '2px solid var(--accent)';
    card.style.outlineOffset = '3px';
    setTimeout(function () { card.style.outline = ''; card.style.outlineOffset = ''; }, 2000);
  });
}());
</script>
</body>
</html>
