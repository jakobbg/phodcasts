# Cached Open Library metadata (refreshed by deleting these files).
cache/metadata/
cache/episodes/
cache/notes/
cache/covers/

# Local constants override copied from config/constants.php.sample.
config/constants.php

# Per-feed notes written locally on the NAS — never tracked.
**/notes.md

# Local IDE metadata.
.idea/

# macOS filesystem metadata.
.DS_Store
